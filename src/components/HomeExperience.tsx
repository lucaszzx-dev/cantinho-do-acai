import { useEffect, useMemo, useRef, useState } from 'react'
import type { Product } from '../types/domain'
import { formatCurrency } from '../utils/format'

interface Props { products: Product[]; onChoose: (product: Product) => void }
const showcaseIds = ['acai-tradicional', 'acai-morango-creme-avela', 'acai-ferrero']
export function HomeExperience({ products, onChoose }: Props) {
 const showcase=useMemo(()=>showcaseIds.map(id=>products.find(p=>p.id===id)).filter((p):p is Product=>Boolean(p)),[products]); const storyProduct=useMemo(()=>products.find(p=>p.id==='acai-tradicional')??products[0],[products]); const [selectedId,setSelectedId]=useState('acai-morango-creme-avela'); const story=useRef<HTMLElement>(null)
 useEffect(()=>{const node=story.current;if(node)node.dataset.stage='start'},[])
 const current=showcase.find(p=>p.id===selectedId)??showcase[0]; if(!current||!storyProduct)return null
 return <><section className="hero-art" aria-label="Cantinho do Açaí"><img src="/cantinho-do-acai-hero.png" width="1536" height="1024" fetchPriority="high" alt="Cantinho do Açaí: donos e açaí artesanal"/><a className="hero-hotspot hero-hotspot--menu" href="#cardapio" aria-label="Ver cardápio"/><button type="button" className="hero-hotspot hero-hotspot--order" onClick={()=>onChoose(storyProduct)} aria-label="Pedir agora"/></section><section className="product-story" ref={story} aria-label="Açaí Tradicional"><div className="product-story__sticky"><div className="product-story__copy"><p className="home-hero__eyebrow">Açaí Tradicional</p><h2>Monte, escolha, aproveite.</h2><p>Monte o tradicional do seu jeito, com os acompanhamentos que você mais gosta.</p><button type="button" className="button button--primary" onClick={()=>onChoose(storyProduct)}>Montar Açaí Tradicional</button></div><div className="product-story__visual"><div className="product-story__media"><span className="product-story__orb"/><span className="product-story__ring"/><img src={storyProduct.image} width="600" height="600" alt={storyProduct.name}/></div></div></div></section><section className="home-showcase" aria-labelledby="destaques"><div className="home-showcase__heading"><p className="home-hero__eyebrow">Escolhas da casa</p><h2 id="destaques">Tem um Cantinho pra cada vontade.</h2></div><div className="home-showcase__products">{showcase.map(product=><button type="button" key={product.id} className={`showcase-product ${product.id===current.id?'is-selected':''}`} onMouseEnter={()=>setSelectedId(product.id)} onFocus={()=>setSelectedId(product.id)} onClick={()=>setSelectedId(product.id)} aria-pressed={product.id===current.id}><img src={product.image} width="600" height="600" alt={product.name} loading="lazy"/><span>{product.name}</span><small>{product.fromPrice?'A partir de ':''}{formatCurrency(product.price)}</small>{product.id===current.id&&<span className="showcase-product__details">{product.description??product.subtitle}<b>Escolher</b></span>}</button>)}</div><article className="home-feature"><img src={current.image} width="600" height="600" alt={current.name} loading="lazy"/><div><p className="home-hero__eyebrow">Em destaque</p><h3>{current.name}</h3><p>{current.description??current.subtitle}</p><strong>{current.fromPrice?'A partir de ':''}{formatCurrency(current.price)}</strong><button type="button" className="button button--primary" onClick={()=>onChoose(current)}>Escolher</button></div></article></section></>
}
