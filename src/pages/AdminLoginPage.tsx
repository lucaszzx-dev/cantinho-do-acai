import { useEffect, useState } from 'react'
import { apiUrl } from '../api/client'
const baseUrl = apiUrl
export function AdminLoginPage() { const [email, setEmail] = useState(''); const [password, setPassword] = useState(''); const [error, setError] = useState(''); const [loading, setLoading] = useState(false); useEffect(() => { fetch(`${baseUrl}/api/admin/auth/me`, { credentials: 'include' }).then((r) => { if (r.ok) window.location.assign('/admin') }) }, []); const submit = async (e: React.FormEvent) => { e.preventDefault(); setLoading(true); setError(''); const r = await fetch(`${baseUrl}/api/admin/auth/login`, { method: 'POST', credentials: 'include', headers: { 'content-type': 'application/json' }, body: JSON.stringify({ email, password }) }); setLoading(false); if (!r.ok) return setError('E-mail ou senha inválidos.'); window.location.assign('/admin') }; return <main className="admin-page"><section className="admin-panel"><h1>Entrar no painel</h1><form onSubmit={submit}><label>E-mail<input type="email" value={email} onChange={(e) => setEmail(e.target.value)} required /></label><label>Senha<input type="password" value={password} onChange={(e) => setPassword(e.target.value)} required /></label>{error && <p className="form-error">{error}</p>}<button className="button button--primary" disabled={loading}>{loading ? 'Entrando…' : 'Entrar'}</button></form></section></main> }
