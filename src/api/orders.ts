const baseUrl = import.meta.env.VITE_API_URL ?? 'http://localhost:3000'
export type OrderInput = { idempotencyKey: string; customerId?: string; customerName: string; phone: string; address: string; addressNumber: string; complement?: string; neighborhood: string; notes?: string; paymentMethod: string; items: { productId: string; variantId?: string; quantity: number; selections: Record<string, string[]> }[] }
export async function createOrder(input: OrderInput) { const response = await fetch(`${baseUrl}/api/orders`, { method: 'POST', headers: { 'content-type': 'application/json' }, body: JSON.stringify(input) }); if (!response.ok) throw new Error('Não foi possível registrar o pedido. Tente novamente.'); return response.json() as Promise<{ orderNumber: number; publicAccessToken: string; status: string }> }
export async function getOrder(token: string) { const response = await fetch(`${baseUrl}/api/orders/public/${token}`); if (!response.ok) throw new Error('Pedido não encontrado.'); return response.json() as Promise<any> }
